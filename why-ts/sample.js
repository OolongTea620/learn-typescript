// function sum(a, b) {
//     return a + b;
// }
// var result = sum(10, 20); //return '1020'

// @ts-check 

/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */
function sum(a, b) {
    return a + b;
}
sum(10, 20); // 코드에 대한 정의를 보여주지만 타입 검사는 안해준다